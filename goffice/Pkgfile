# Description: library of document-centric objects and utilities building on top of GLib and Gtk+
# URL: http://www.gnome.org
# Maintainer: anton baranov, abaranov dot anton at gmail dot com	
# Depends on: gtk libxml gconf pango cairo libgsf

name=goffice
version=0.8.9
release=1
source=(ftp://ftp.gnome.org/pub/GNOME/sources/$name/0.8/$name-$version.tar.bz2)

build(){
	cd $name-$version

	sed -i -e "s/USE_NLS=yes/USE_NLS=no/" \
			 -e "s/ENABLE_NLS 1/ENABLE_NLS 0/" configure

	./configure --prefix=/usr \
					--disable-gtk-doc 

	make 
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share/gtk-doc

}
