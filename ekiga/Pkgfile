# Description: Open-source VoIP and video conferencing application for GNOME
# URL: http://ekiga.org/
# Maintainer: anton baranov, abaranov dot anton at gmail dot com	
# Depends on: libsigc++ libxml2 gtk gconf libnotify avahi dbus-glib opal

name=ekiga
version=3.2.7
release=1
source=(http://ftp.gnome.org/pub/gnome/sources/$name/3.2/$name-$version.tar.bz2)

build(){
	cd $name-$version

	sed -i -e "s/USE_NLS=yes/USE_NLS=no/" \
			 -e "s/ENABLE_NLS 1/ENABLE_NLS 0/" configure


	./configure --prefix=/usr \
					--mandir=/usr/man \
					--disable-eds \
					--disable-gdu \
					--disable-scrollkeeper \
					--disable-opal-debug \
					--disable-h323 \
					--disable-nls 

	make 
	make DESTDIR=$PKG install
}
