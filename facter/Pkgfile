# Description: Library to provide information about the hardware and OS to the puppet master server
# URL: http://www.puppetlabs.com/
# Maintainer: anton baranov, abaranov dot anton at gmail dot com	
# Depends on: ruby docutils

name=facter
version=2.0.1
release=1
source=(http://puppetlabs.com/downloads/$name/$name-$version.tar.gz)

build(){
	cd $name-$version
	ruby ./install.rb  \
							--no-rdoc \
							--mandir=/usr/man \
							--destdir=$PKG

}
