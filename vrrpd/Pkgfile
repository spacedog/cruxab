# Description: Implementation of Virtual Router Redundancy Protocol 
# URL: http://sourceforge.net/projects/vrrpd/
# Maintainer: anton baranov, abaranov dot anton at gmail dot com	
# Depends on: 

name=vrrpd
version=1.0
release=1
source=(http://downloads.sourceforge.net/project/$name/$name/1.0/$name-$version.tar.gz)

build(){
	cd $name-$version

	make

	install -D -m 755 -o root -g root $name $PKG/usr/sbin/$name
	install -D -m 644 -o root -g root $name.8 $PKG/usr/man/man8/$name.8
}
