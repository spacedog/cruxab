# Description: Various plugins for remmina	
# URL:		http://remmina.sourceforge.net
# Maintainer:	anton baranov, abaranov dot anton at gmail dot com
# Depends on:  remmina freerdp nxclient gnutls
name=remmina-plugins
version=0.9.2
release=1

source=(http://downloads.sourceforge.net/project/remmina/0.8/$name-$version.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr --disable-nls --disable-telepathy
	make 
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/info
	rm -rf $PKG/usr/share/doc

}
