# Description: Open source platform for IT systems management
# URL: http://www.puppetlabs.com/
# Maintainer: anton baranov, abaranov dot anton at gmail dot com	
# Depends on: facter

name=puppet 
version=2.7.1
release=1
source=(http://www.puppetlabs.com/downloads/$name/$name-$version.tar.gz)

build(){
	cd $name-$version

	ruby install.rb --no-rdoc \
						 --destdir=$PKG
}
